<%= simple_form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>
      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <fieldset>
    <%= f.input :name, input_html: {class: "avg"} %>
    <%= f.input :gender, collection: Item::GENDER_LIST, as: :radio_buttons, input_html: {class: "avg"} %>
    <%= f.input :age, collection: Item::AGE_LIST, input_html: {class: "avg"} %>
    <%= f.input :quantity, input_html: {class: "avg", value: 1 } %>
    <%= f.input :category_id, collection: Category.all, input_html: {class: "avg"} %>
    <%= f.input :notes, as: :text, input_html: { class: "avg" } %>
    <%= f.input :donated, as: :boolean, boolean_style: :inline %>
    <div class="actions">
      <%= f.submit %>
    </div>
  </fieldset>
<% end %>
